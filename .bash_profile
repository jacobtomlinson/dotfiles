# dotfiles by Jacob Tomlinson
# https://github.com/jacobtomlinson/dotfiles

# DON'T MODIFY THIS FILE
# Place new config in a separate file within ~/.bashrc.d/

# Source .bashrc if it exists (which will source everything in ~/.bashrc.d/)
if [ -f ~/.bashrc ]; then
  source ~/.bashrc
fi

# added by Miniconda3 4.3.11 installer
export PATH="/Users/jacob/.miniconda3/bin:$PATH"
